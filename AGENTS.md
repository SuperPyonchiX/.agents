# AGENTS.md

## このリポジトリについて

Agent Skills（`SKILL.md`）の**実体置き場**であり、アプリケーションコードではない。
成果物はすべて Markdown とそれを補助するスクリプトで、ビルドもテストスイートも存在しない。

```
skills/<skill-name>/SKILL.md   ← 各スキルの本体
```

`~/.claude/skills` はここへのシンボリックリンク。**リンク自体をこのリポジトリに追加しないこと。**

## 最重要

**スキルを新規作成・改修するときは、必ず `workflow-skill-architect` スキルを使う。**
「SKILL.md を書いて」「このワークフローをスキル化して」に加え、複数ステップ・反復・並列処理を伴う作業を再利用可能にしたい、という話が出た時点で該当する。「スキル」という語が明示されていなくても使う。

## SKILL.md の規約

- frontmatter に置いてよいのは `name` / `description` / `license` / `allowed-tools` / `metadata` / `compatibility` のみ。基本は `name` と `description` の2つ。
- **`name` はディレクトリ名と完全一致させる。** kebab-case、64文字以内。ずれると読み込まれず、エラーも出ない。
- **`description` には「何をするか」と「いつ使うか」を両方書く。** 発火判断の唯一の材料であり、「いつ使うか」がないと拾われない。1024文字以内、山括弧は使わない。
- 本文は500行以内。超えたら `references/` に分割し、SKILL.md からは索引として参照する。
- 指示は命令形で書く。完了条件は判定可能な形にする（「確認する」ではなく「一覧を提示し確認を得たこと」）。想定外時の戻り条件と、抜け道を塞ぐ禁止事項も書く。

## ファイル配置の規約

| ディレクトリ | 用途 |
| --- | --- |
| `references/` | 大部の資料。SKILL.md から索引として参照し、必要なものだけ読ませる |
| `scripts/` | 決定論的な処理。毎回同じ結果でなければ困るものはここに逃がす |
| `assets/` `templates/` | 出力テンプレート。構造を厳密に維持させる用 |
| `examples/` | 出力例 |

- **SKILL.md から参照されないファイルを置かない。** 孤児ファイルは検証で警告になる。
- `scripts/` にファイルを追加したら、呼び出し方と終了コードの意味を SKILL.md か `references/` に必ず書く。

## 移植性の規約

このリポジトリのスキルは Claude Code 以外（Codex / Gemini CLI / Copilot）でも使う前提。
**Claude Code 独自拡張（サブエージェント実行、動的コンテキスト注入、呼び出し制御）は使わない。**
標準仕様の範囲（`scripts/` `references/` `assets/` の同梱）に留める。

スクリプトが**特定のディレクトリ配置を前提にしない**ことは必須。スキルディレクトリからの相対パスと
引数で受け取ったパスだけで動くようにする。

外部パッケージは使ってよい。標準ライブラリで無理なく書ける処理をわざわざ外部パッケージに頼る必要は
ないが、`openpyxl` のように**自前実装すると検証コストのほうが高くつく領域では素直に使う**。
条件は次の3つ。

- 依存するパッケージ名を SKILL.md の「スクリプト」節に明記する
- import に失敗したら**終了コード 2 で止め、導入方法を案内する**。黙って落ちない
- 広く使われている単機能のパッケージに限る。フレームワークや、特定サービスに紐づくものは避ける

依存が無いスクリプトと有るスクリプトが混在してよい。**依存を1本のスクリプトに閉じ込められるなら
そうする**（`static-analysis-triage` は Excel I/O の2本だけが `openpyxl` に依存し、クラスタリングと
検査は標準ライブラリのみで動く）。こうしておくと、パッケージを入れられない環境でも一部が使える。

## 変更後に必ず実行すること

```bash
python skills/workflow-skill-architect/scripts/validate_skill.py skills/<name>
```

終了コード0（合格）になるまで直す。**ERROR が残ったままコミットしない。**

合格は「機械判定できる不備がゼロ」という意味でしかない。主観項目は
`skills/workflow-skill-architect/references/review-checklist.md` を目視で当てる。

## 記述言語

日本語。既存3スキルに合わせる。
`description` のみ、他ツールでの発火精度のため英語併記を許容する。

## コミット

- 1スキル1コミット。
- メッセージは `add: <skill-name> スキルを追加` / `fix: <skill-name> の description を修正` / `docs: ...` の形式。
- シンボリックリンクをコミットしない。Windows でチェックアウトすると実体化されず壊れる。

## 人間向けの運用手順

セットアップ、トラブルシューティング、チーム展開への移行手順は @README.md にある。
