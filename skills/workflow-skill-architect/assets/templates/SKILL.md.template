---
name: 〔スキル名。ディレクトリ名と一致させる。英小文字・数字・ハイフンのみ、64文字以内〕
description: 〔何をするスキルか。1〜2文〕。〔ユーザーが使いそうな日本語の依頼文言を 3〜6 個列挙〕で使う。〔紛れやすい隣接スキルがあり、実際に取り違えが起きる場合だけ「〜は skill-name の担当」を1文。英語は併記しない〕
metadata:
  web-description: 〔description の要点と発火語を 200 文字以内に圧縮したもの。新しい文章を書かない〕
---

<!-- description には山括弧（< >）を書いてはいけない。仕様で弾かれる。
     このテンプレートが 〔〕 を使っているのはそのため。埋めたら 〔〕 ごと消すこと。
     description は 1024 文字以内、目安は 300 文字。全スキル分が毎セッション読み込まれる
     固定費なので、「〜といった話が出たら明示的な言及がなくても使う」の類の定型句は書かない。
     metadata.web-description は claude.ai（上限 200 文字）向け。無いと配布用 zip が作れない。
     frontmatter に置いてよいキーは
     name / description / license / allowed-tools / metadata / compatibility のみ。
     この HTML コメントは埋め終わったら削除する。 -->

# <スキル名>

<2〜3行。このスキルは何をするものか、そして設計上の勘所（どこで失敗しやすいか）>

## ワークフロー

<構造に応じてどちらかを書く。両方該当するなら両方>

```mermaid
graph TD
    A[<ノード1>] --> B[<ノード2>]
    A --> C[<ノード3>]
    B --> D[<統合>]
    C --> D
```

| ノード | 依存 | 入力 | 出力 |
|---|---|---|---|
| <name> | — | <path> | <path> |

<ループがある場合>
```
for 周回 in 1..<N>:
    <生成 or 修正>
    <検証>
    if <成功条件>: break
    if <前回と同じ失敗>: <方針を見直す / ユーザーに相談>
else:
    <上限到達。未達であることを明示して報告し、勝手に続行しない>
```

## 手順

### 1. <工程名>
<命令形で。入力 → 何をする → 出力>

### 2. <工程名>
<同上>

## 出力形式

<受け渡す形式のスキーマ + 最小実例。または最終成果物のテンプレート>

## 終了条件と失敗時の扱い

- 成功: <条件>
- 上限: <数値>
- 収束しない場合: <エスカレーション先と、そのとき伝えること>
- 想定外の入力: <止まってユーザーに確認する条件>

## 参照ファイル

| ファイル | 読むタイミング |
|---|---|
| `references/<name>.md` | <いつ読むか。必ず書く> |

## スクリプト

```
python scripts/<name>.py <引数>
```
<終了コードの意味と出力の形式>
