# 不具合解析報告 `<DF-001>`

- 対象: `<リポジトリ / モジュール名>`
- 解析期間: `<開始日 - 完了日>`
- 詳細記録: `work/defect.json` / 仮説台帳: `work/hypotheses.md`
- 結論: `<根本原因を特定 / 未解決（再現せず）/ 未解決（仮説を検証しきったが特定できず）>`

**都合の悪い項目を省かない。** 説明できずに残った観測、暫定処置として入れたもの、水平展開のヒットは必ず書く。

## 1. 現象

| | |
|---|---|
| 期待動作 | `<observable な形で>` |
| 実動作 | `<観測した値・ログ・挙動>` |
| 発生頻度 | `<常時 / 間欠(発生率) / 条件付き(条件)>` |
| 初出 | `<日付 / コミット / リリース。不明なら不明と書く>` |
| 再現性 | `<再現する / 間欠的に再現する / 再現しない>` |

### 環境

| 項目 | 値 |
|---|---|
| ターゲット | `<MCU / ボードリビジョン>` |
| ツールチェーン | `<コンパイラとバージョン>` |
| 最適化 | `<-O2 等。構成差で挙動が変わるなら両方>` |
| ビルド構成 | `<Debug / Release>` |
| RTOS | `<名称とバージョン / ベアメタル>` |

### 再現手順

1. `<...>`
2. `<...>`

最小再現ケース: `<パス、または「作成せず」>`

## 2. 容疑範囲の絞り込み

| | |
|---|---|
| 手法 | `<bisect / log / code-reading / none>` |
| 特定したコミット | `<ハッシュと概要。none なら「絞り込めず」と理由>` |
| コード以外の変更 | `<ツールチェーン更新、ビルド設定、HW リビジョン。無ければ「なし」>` |

## 3. 検証した仮説

| ID | 仮説 | 状態 | 判断の根拠 |
|---|---|---|---|
| HY-001 | `<...>` | confirmed | `<観測>` |
| HY-002 | `<...>` | rejected | `<なぜ違うと判断したか>` |

検証実施 `<n>` 件 / 登録 `<m>` 件（`deferred` `<k>` 件）

## 4. 根本原因

`<「A が B のとき C になるため、D という現象が出る」の形で因果まで書く>`

### 現象を説明しきれているかの検査

| 検査 | 結果 |
|---|---|
| 観測された現象を全て説明できるか | `<可 / 不可（残る観測を列挙）>` |
| 発生頻度を説明できるか | `<可 / 不可>` |
| なぜ今まで出ていなかったかを説明できるか | `<可 / 不可 / 該当なし（最初から存在）>` |
| 環境依存を説明できるか | `<可 / 不可 / 該当なし>` |

根拠: `<観測ログ、逆アセンブル、テスト結果へのパス>`

**説明できずに残った観測**: `<列挙する。無ければ「なし」>`

## 5. 対策

| | |
|---|---|
| 種別 | `<恒久対策 / 暫定処置>` |
| 内容 | `<何をどう直したか>` |
| 追加テスト | `<テストのパスと名前>` |
| RED 確認 | `<修正前に落ちることを確認した / していない>` |
| 既存テスト | `<全パス / 影響あり（内容）>` |

暫定処置の場合は、**根本原因が未確定であること**と、恒久対策に必要な追加調査をここに書く。

## 6. 水平展開

| パターン | grep | ヒット | 該当と判断 |
|---|---|---|---|
| `<PTN-INT-001>` | `<パターン>` | `<n>` | `<m>` |

**ヒット0件だった場合も行を残す。** 実施していないことと区別がつかなくなる。

別途起票した件:

| 起票先 | 内容 |
|---|---|
| `<チケット番号 / DF-002>` | `<...>` |

無ければ「該当なし」と書く。

## 7. 残課題

- `<未解決の観測、次に試すべきこと、監視項目として残したもの>`

未解決で終える場合は、**未解決であることをこの節の冒頭に明示する。** 試した再現手順・検証した仮説・観測できた事実・次に試すべきことを列挙する。
