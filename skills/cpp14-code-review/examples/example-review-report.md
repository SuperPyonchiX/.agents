# コードレビュー報告

- 対象: vehicle-ecu / motor モジュール
- mode: diff（比較元: main、変更 3 ファイル / 78 行）
- 実施日: 2026-08-11
- 台帳: `work/review-log.md`

## 結論

重大度「高」が 2 件ある。いずれも異常系の扱いに関するもので、うち RV-002 は設定失敗時も回転指令が出る経路を作っている。修正なしのマージは推奨しない。それ以外は保守性の指摘であり、後追いで差し支えない。

差分外に「高」が 1 件（RV-007）ある。今回の変更とは無関係だが、割込みと共有する変数の排他が無いため、別課題として起票を提案する。

## 指摘の集計

| 重大度 | 差分 | 差分外 | 合計 |
|---|---|---|---|
| 高 | 2 | 1 | 3 |
| 中 | 3 | 1 | 4 |
| 低 | 2 | 0 | 2 |

| 状態 | 件数 |
|---|---|
| open | 0 |
| fixed | 0 |
| accepted | 1 |
| rejected | 8 |

※ 指摘 9 件のうち 8 件は `open` としてユーザーに提示したのち、修正方針の合意を得て `fixed` に更新済み。

`python scripts/check_review_log.py work/review-log.md` の結果: 終了コード 0

## 重大度「高」の指摘（全件）

| ID | 対象 | 指摘 | 是正案 |
|---|---|---|---|
| RV-001 | src/motor_controller.cpp:42 | `(int)raw` は double からの縮小変換で、`raw` が `INT32_MAX` を超えると未定義動作になる（CAST-001） | 範囲を検査したうえで `static_cast<std::int32_t>` にする |
| RV-002 | src/motor_controller.cpp:58 | `SetSpeed()` の戻り値を確認せずに次へ進んでいる。設定失敗時も回転指令が出るため、意図しない速度で動作する | 戻り値が false なら `Stop()` して早期 return する |
| RV-007 | src/motor_controller.cpp:210（**差分外**） | 割込みハンドラと共有する `g_sensor_raw` が `volatile` のみで、32bit 読み出しの原子性が保証されていない | 割込み禁止区間で読むか、二重読み出しで一致を確認する。**別課題として起票を提案** |

## 機械スキャンの結果

| 確度 | 候補 | 採用 | 不採用 |
|---|---|---|---|
| high | 4 | 4 | 0 |
| medium | 11 | 3 | 8 |
| policy | 0 | 0 | 0 |
| 外部ツール | 7 | 5 | 2 |

不採用の代表理由:

- `TYPE-001` のうち 5 件は、ベンダ提供の `hal_motor.h` のシグネチャに合わせた宣言であり、固定幅型に変えると型が合わない
- `MNT-002` のうち 3 件はビットシフト量とレジスタのビット位置で、定数化しても意味が増えない
- 外部ツールの 2 件は生成コード（`build/gen/`）に対する指摘で、レビュー対象外

静的解析ツール: clang-tidy 18.1（`autosar-*`, `cert-*` チェッカ有効）
取り込んだ指摘: 7 件

## 目視レビューの観点別所見

| 観点 | 所見 | 該当指摘 |
|---|---|---|
| 設計意図との一致 | `MotorController::Update` の Doxygen `@retval` に false の条件が追記されておらず、実装だけ変わっている | RV-003 |
| エラー処理と異常系 | 戻り値の握りつぶしが1件。除算・境界検査は全経路にあることを確認 | RV-002 |
| 責務と構造 | `Update()` が 88 行。状態遷移・センサ読み取り・ログの3責務 | RV-004 |
| リソースとライフタイム | 動的確保なし。ハンドルの解放が全 return 経路にあることを確認 | — |
| 割込み・並行安全性 | `g_sensor_raw` の排他が不足。他の共有変数は割込み禁止区間内 | RV-007 |
| 実行時間とスタック | ループは全て定数上限。最大ローカル配列 64 バイト。再帰なし | — |
| 移植性とハードウェア依存 | レジスタアクセスは `hal_motor.cpp` に閉じている。エンディアン依存なし | — |
| テスト十分性 | `SetSpeed` の上限値ちょうど（`kMaxRpm`）のテストがない | RV-005 |
| 可読性と命名 | `timeout` に単位がない。`val` が設計用語（目標回転数）と対応していない | RV-006, RV-008 |

## 逸脱承認（accepted）

| ID | 対象 | 承認した理由 | 承認者 |
|---|---|---|---|
| RV-009 | src/motor_controller.cpp:120 | `3000` はハードウェア仕様が直接要求する固定値で、定数名を付けても意味が増えない。コード側に `@note` で出典を追記することを条件に据え置き | ksk01 / 2026-08-11 |

## 当てられなかった観点・未実施の項目

- `Shutdown()` の呼び出し元が `app/` 配下にあり今回のレビュー範囲外のため、`@pre`（Initialize 済みであること）が守られているかは**判定できていない**。範囲に含めるかは要判断
- 単体テストの実行はしていない。テスト十分性は**コードを読んだ範囲**での判断であり、カバレッジは測っていない

## ユーザーに判断を仰ぐ事項

- RV-007（差分外・重大度「高」）を今回の修正に含めるか、別課題として起票するか
- `Shutdown()` の呼び出し元をレビュー範囲に追加するか
