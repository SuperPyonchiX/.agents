# スキル置き場 横断検査報告

- 対象: `<skills ディレクトリのパス>`
- 実施日: `<2026-08-29>`
- 機械検査の生データ: `work/audit-report.json`

**実行できなかった検査を「問題なし」として書かない。** 渡せなかった引数があるなら次の表に残す。

| 検査 | 実施 | 備考 |
|---|---|---|
| web-description / frontmatter | 済 | — |
| 発火競合の候補抽出 | 済 | — |
| 個別検証の集約（`--validator`） | `<済 / 未実施>` | `<未実施ならその理由>` |
| README 突合（`--readme`） | `<済 / 未実施>` | `<同上>` |

## 1. 集計

| | 件数 |
|---|---|
| 検査したスキル | `<n>` |
| ERROR | `<n>` |
| WARN | `<n>` |
| 競合候補 | `<n>` 組（判定 `<m>` 組 / 未判定 `<k>` 組） |

## 2. ERROR（判断を要さない不備）

**`web-description` の欠落は配布が止まる。** 個別に挙げる。

| スキル | 内容 | 是正案 |
|---|---|---|
| `<name>` | `<metadata.web-description がない>` | `<200文字以内の短縮版を追加する>` |

無ければ「なし」と書く。

## 3. 競合候補の判定

処置は `範囲を狭める` / `棲み分けを書く` / `統合する` / `分割する` / `問題なし` / `要検討` のいずれか。
**「問題なし」にも理由を書く。** 次回同じ組を見たときに同じ結論へ到達できる形にすること。

| ペア | score | 処置 | 理由 |
|---|---|---|---|
| `<a>` / `<b>` | `<4.24>` | 問題なし | `<成果物が異なり、双方が名指しで振り分けている>` |
| `<c>` / `<d>` | `<2.78>` | 棲み分けを書く | `<d 側に a の名前が出てこない。末尾に1文を追加する>` |

### 未判定として残した候補

上限を超えたため判定していないもの。**黙って切り捨てない。**

| ペア | score |
|---|---|
| `<e>` / `<f>` | `<1.20>` |

無ければ「なし」と書く。

## 4. README 突合

| 項目 | 内容 |
|---|---|
| 表にあって実体が無い | `<列挙 / なし>` |
| 実体にあって表に無い | `<列挙 / なし>` |
| 行数のズレ | `<スキル名: 表 n 行 / 実体 m 行 / なし>` |

## 5. 是正の要否

ここで**ユーザーの判断を仰ぐ**。確認を得ずに A4 へ進まない。

| # | 是正対象 | 内容 | 判断 |
|---|---|---|---|
| 1 | `<name>` の web-description | `<追加する>` | `<未回答 / 実施 / 見送り>` |
| 2 | `<name>` の description | `<末尾に棲み分けの1文を足す>` | `<未回答 / 実施 / 見送り>` |

description の変更は**変更前後を並べて個別に了承を得る**。発火挙動が変わる。

## 6. 是正の結果（A4 を実施した場合）

| # | 対象 | 実施内容 | 再検査 |
|---|---|---|---|
| 1 | `<name>` | `<...>` | `<ERROR が n 件から m 件へ>` |

再実行の結果:

| | 是正前 | 是正後 |
|---|---|---|
| ERROR | `<n>` | `<m>` |
| WARN | `<n>` | `<m>` |

**新規 ERROR が出ていないこと**を確認する。出ていれば A2 に戻る。

## 7. 残課題

- `<未判定の候補、見送った是正、次回に持ち越す観点>`
